<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid
        android:color="@color/colorPrimaryBlue"
        />
    <stroke
        android:width="3dp"
        android:color="@color/colorText"
        />
    <corners
        android:topLeftRadius="@dimen/corner_login"
        android:topRightRadius="@dimen/corner_login"
        android:bottomRightRadius="@dimen/corner_login"
        android:bottomLeftRadius="@dimen/corner_login"/>
</shape>

